<h3>Edit your Review</h3>

<%= render partial: "reviews/form", locals: { product: @product, review: @review } %>
<%= link_to 'Delete', product_review_path(@review), method: :delete, data: {confirm: "Are you sure you want to delete this review'? This cannot be undone!"} %>