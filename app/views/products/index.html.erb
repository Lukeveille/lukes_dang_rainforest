<h1>INDEX</h1>

<% @products.each do |product|%>
  <h2><%=product.name%></h2>
  <p><%=product.description%></p>
  <p><%=product.price%></p>
  <%= link_to "Show Product", product_path(product) %>
<% end %>